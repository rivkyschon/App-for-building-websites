const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "localhost",
      user: "root",
      password: "Toor22",
      database: 'buildPortfolio'
    }
};


module.exports = config;
